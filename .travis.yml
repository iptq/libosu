language: rust
rust:
  - stable
  - beta
  - nightly
os: linux
before_install:
  - sudo apt-get update -y
  - sudo apt-get install -y libgtk-3-dev
script:
  - cargo build --package libosu
  - cargo test --package libosu

